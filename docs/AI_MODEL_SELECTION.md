# AI Model Selection

- Refresh models from provider APIs
- Searchable picker with exact API model IDs
- Separate OCR / matching / learning selections
- Requested vs actual model always audited
- Fallbacks disabled by default; never silent
